import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.SUPABASE_SERVICE_ROLE_KEY!); async function run() { console.log('Inserting...'); const { data, error } = await supabase.from('personas').insert({ nombre: 'Juan Test Insert', tipo_persona: 'FISICA', origen_dato: 'IA_OCR' }).select(); console.log('Insert Result:', error || 'SUCCESS', data); } run();
